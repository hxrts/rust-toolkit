# Guide

Broken link to [missing](docs/missing.md).

Use `just missing`.

See `crates/traits/src/missing.rs`.

The symbol `UnknownType` is not real.

Bad emoji ✅.

This sentence uses a semicolon; the prose checker should reject it.

```rust
let retries = 3;
```

- Missing explanatory prose on purpose.
