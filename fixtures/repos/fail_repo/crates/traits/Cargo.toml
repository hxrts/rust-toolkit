[package]
name = "fixture-traits"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
fixture-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
lazy_static = "1"
