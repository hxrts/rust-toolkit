# Guide

Use `just check`.

See `Cargo.toml` and `crates/traits/src/lib.rs`.

The symbol `SampleTrait` is real.

```rust
let retries = 3;
```

This paragraph explains the example and keeps prose dominant.
