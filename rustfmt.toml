# Toolkit rustfmt configuration.
#
# This file is intended to be portable across repositories that opt into the
# toolkit's formatting policy. It uses nightly-only rustfmt settings and should
# be run through the toolkit nightly shell.

unstable_features = true

max_width = 88
use_small_heuristics = "Max"

fn_params_layout = "Tall"
fn_call_width = 60
struct_lit_width = 40
struct_variant_width = 40
enum_discrim_align_threshold = 20
empty_item_single_line = true

imports_granularity = "Crate"
group_imports = "StdExternalCrate"

chain_width = 60
format_code_in_doc_comments = true

match_block_trailing_comma = true
match_arm_blocks = true
match_arm_leading_pipes = "Always"

trailing_comma = "Vertical"

where_single_line = false

blank_lines_upper_bound = 1
blank_lines_lower_bound = 0

reorder_impl_items = true
use_field_init_shorthand = true

wrap_comments = true
comment_width = 80
